<Window x:Class="AutoCheckMechanical.SapConnectionWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Logon SAP"
        Height="480"
        Width="460"
        WindowStartupLocation="CenterOwner"
        WindowStyle="ToolWindow"
        ResizeMode="NoResize"
        Background="#3B3B3B"
        FontFamily="Segoe UI">

    <Window.Resources>
        <!-- Campos de texto/senha no estilo da tela de logon do SAP GUI: sem
             preenchimento, apenas um contorno fino cinza-claro sobre o fundo
             escuro. TextBoxBase é abstrata, não pode ser TargetType de
             Style diretamente, por isso TextBox e PasswordBox têm estilos
             próprios (duplicados) em vez de compartilhar um só. -->
        <Style x:Key="SapFieldStyle" TargetType="TextBox">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="CaretBrush" Value="White"/>
            <Setter Property="BorderBrush" Value="#8C8C8C"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="6,4"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Height" Value="28"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
        </Style>

        <Style x:Key="SapPasswordFieldStyle" TargetType="PasswordBox">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="CaretBrush" Value="White"/>
            <Setter Property="BorderBrush" Value="#8C8C8C"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="6,4"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Height" Value="28"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
        </Style>

        <Style TargetType="ComboBoxItem">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Padding" Value="8,6"/>
            <Style.Triggers>
                <Trigger Property="IsHighlighted" Value="True">
                    <Setter Property="Background" Value="#2F6FBD"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="SapComboStyle" TargetType="ComboBox">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#8C8C8C"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="6,4"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Height" Value="28"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Grid>
                            <ToggleButton x:Name="toggle"
                                          IsChecked="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}"
                                          ClickMode="Press">
                                <ToggleButton.Template>
                                    <ControlTemplate TargetType="ToggleButton">
                                        <Border Background="{TemplateBinding Background}"
                                                BorderBrush="{Binding BorderBrush, RelativeSource={RelativeSource AncestorType=ComboBox}}"
                                                BorderThickness="{Binding BorderThickness, RelativeSource={RelativeSource AncestorType=ComboBox}}">
                                            <Grid>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="*"/>
                                                    <ColumnDefinition Width="20"/>
                                                </Grid.ColumnDefinitions>
                                                <Path Grid.Column="1" Data="M0,0 L5,5 L10,0 Z" Fill="White"
                                                      HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                            </Grid>
                                        </Border>
                                    </ControlTemplate>
                                </ToggleButton.Template>
                            </ToggleButton>
                            <TextBlock Margin="{TemplateBinding Padding}"
                                       Text="{Binding SelectedItem.Exibicao, RelativeSource={RelativeSource AncestorType=ComboBox}}"
                                       Foreground="{TemplateBinding Foreground}"
                                       IsHitTestVisible="False"
                                       VerticalAlignment="Center"/>
                            <Popup x:Name="popup" IsOpen="{TemplateBinding IsDropDownOpen}"
                                   Placement="Bottom" AllowsTransparency="True" Focusable="False" PopupAnimation="Slide">
                                <Border Background="#2E2E2E" BorderBrush="#8C8C8C" BorderThickness="1"
                                        MinWidth="{Binding ActualWidth, RelativeSource={RelativeSource AncestorType=ComboBox}}">
                                    <ScrollViewer Margin="2" SnapsToDevicePixels="True">
                                        <ItemsPresenter/>
                                    </ScrollViewer>
                                </Border>
                            </Popup>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="SapLoginButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="White"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="bd"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="bd" Property="Background" Value="#4A4A4A"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.5"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="SapHelpButtonStyle" TargetType="Button">
            <Setter Property="Foreground" Value="#C9C9C9"/>
            <Setter Property="BorderBrush" Value="#8C8C8C"/>
            <Setter Property="Width" Value="26"/>
            <Setter Property="Height" Value="26"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Grid>
                            <Ellipse Stroke="{TemplateBinding BorderBrush}" StrokeThickness="1" Fill="Transparent"/>
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="CampoRotuloStyle" TargetType="TextBlock">
            <Setter Property="Foreground" Value="#C9C9C9"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>

        <Style x:Key="LinkButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="#9A9A9A"/>
            <Setter Property="FontSize" Value="11"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Foreground" Value="White"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid>
        <StackPanel Margin="34,10,34,10" VerticalAlignment="Center">

            <!-- Logo (aproximação do logo SAP, sem usar a arte oficial) -->
            <Border Background="Black" Width="170" Height="64" HorizontalAlignment="Center" Margin="0,0,0,24">
                <Grid>
                    <Polygon Points="14,10 156,10 142,54 0,54" Fill="White"/>
                    <TextBlock Text="SAP" FontFamily="Arial Black" FontSize="24" FontWeight="Bold" Foreground="Black"
                               HorizontalAlignment="Center" VerticalAlignment="Center" Margin="0,4,10,0"/>
                </Grid>
            </Border>

            <Grid Margin="0,0,0,10">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="90"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <TextBlock Text="System" Style="{StaticResource CampoRotuloStyle}"/>
                <ComboBox Grid.Column="1"
                          Style="{StaticResource SapComboStyle}"
                          ItemsSource="{Binding SistemasDisponiveis}"
                          SelectedItem="{Binding SistemaSelecionado}"
                          DisplayMemberPath="Exibicao"/>
            </Grid>

            <Grid Margin="0,0,0,10">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="90"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <TextBlock Text="Client" Style="{StaticResource CampoRotuloStyle}"/>
                <TextBox Grid.Column="1" Style="{StaticResource SapFieldStyle}"
                         Text="{Binding Client, UpdateSourceTrigger=PropertyChanged}"/>
            </Grid>

            <Grid Margin="0,0,0,10">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="90"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <TextBlock Text="User" Style="{StaticResource CampoRotuloStyle}"/>
                <TextBox Grid.Column="1" Style="{StaticResource SapFieldStyle}"
                         Text="{Binding Usuario, UpdateSourceTrigger=PropertyChanged}"/>
            </Grid>

            <Grid Margin="0,0,0,10">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="90"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <TextBlock Text="Password" Style="{StaticResource CampoRotuloStyle}"/>
                <Grid Grid.Column="1">
                    <PasswordBox Name="txtSenha" Style="{StaticResource SapPasswordFieldStyle}"/>
                    <TextBlock Text="&#x26A0;" Foreground="#D9A227" FontSize="13"
                               HorizontalAlignment="Right" VerticalAlignment="Center"
                               Margin="0,0,8,0" IsHitTestVisible="False"/>
                </Grid>
            </Grid>

            <Grid Margin="0,0,0,20">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="90"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <TextBlock Text="Language" Style="{StaticResource CampoRotuloStyle}"/>
                <TextBox Grid.Column="1" Style="{StaticResource SapFieldStyle}"
                         Text="{Binding Idioma, UpdateSourceTrigger=PropertyChanged}"/>
            </Grid>

            <Button Content="LOGIN"
                    Width="190" Height="38"
                    HorizontalAlignment="Center"
                    Style="{StaticResource SapLoginButtonStyle}"
                    Command="{Binding TestConnectionCommand}"/>

            <TextBlock Text="{Binding StatusText}"
                       Foreground="#C9C9C9"
                       TextWrapping="Wrap"
                       TextAlignment="Center"
                       Margin="0,14,0,0"
                       FontSize="12"/>

            <Button Content="Esquecer login salvo"
                    Style="{StaticResource LinkButtonStyle}"
                    HorizontalAlignment="Center"
                    Margin="0,10,0,0"
                    Command="{Binding EsquecerLoginCommand}"/>
        </StackPanel>

        <Button Content="?"
                Style="{StaticResource SapHelpButtonStyle}"
                HorizontalAlignment="Right"
                VerticalAlignment="Bottom"
                Margin="0,0,16,16"/>
    </Grid>
</Window>

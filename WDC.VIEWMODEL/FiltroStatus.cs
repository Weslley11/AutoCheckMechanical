namespace WDC.VIEWMODEL
{
    // Filtro rápido da tabela de resultados (MainWindow), além do filtro por
    // texto (FiltroTexto) já existente.
    public enum FiltroStatus
    {
        Todos,
        ComErro,
        SemErro,
        Pendente
    }
}
